# Rohmaterial

Der vollständige Workshop-Thread sollte lokal hier abgelegt werden, aber nicht ins öffentliche Repository gepusht werden.

Empfohlene lokale Datei:

```text
ui-konzeption-mit-vue-thread-raw.md
```

Empfehlung für `.gitignore`:

```gitignore
docs/phase-5/000_raw/
```
